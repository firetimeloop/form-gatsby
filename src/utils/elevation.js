import { buttonShadowColor } from "./tokens/colors";

const elevation = [
  `box-shadow: inset 0 7px 9px -7px ${buttonShadowColor}`,
  `box-shadow: 0 4px 8px ${buttonShadowColor}`,
  `box-shadow: 0 2px 4px ${buttonShadowColor}`,
  `box-shadow: 0 4px 8px ${buttonShadowColor}`,
];

export default elevation;
